# SlotSwapper - Client

A minimal React + Vite client for the SlotSwapper challenge.

Setup:
1. npm install
2. npm run dev

The client expects the API at http://localhost:4000/api by default. You can override via VITE_API_BASE environment variable.
